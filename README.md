# SurveyDash

## Usage

Assume you have a list of papers. Place their title in a text file, one title per line. Then run the following command:

```bash
python main.py --survey "your survey topic" --papers "path/to/the/titles.txt"
```
